server:
  port: 8081
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: scraping_db
      auto-index-creation: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
